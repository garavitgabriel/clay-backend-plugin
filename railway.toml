[build]
builder = "NIXPACKS"

[build.nixpacks]
installCmd = "pip install -r hosted/requirements.txt"
startCmd = "cd hosted && uvicorn app:app --host 0.0.0.0 --port ${PORT:-8000}"

[deploy]
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
