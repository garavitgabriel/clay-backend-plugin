{
  "name": "clay-backend-plugin",
  "description": "Store, search, and analyze patterns across your Clay data. Import CSV/JSON exports and let Claude find insights across records.",
  "version": "0.2.0",
  "author": {
    "name": "Gabriel Garavit"
  },
  "license": "MIT",
  "keywords": ["clay", "aggregation", "embeddings", "sales", "analysis"],
  "userConfig": {
    "CLAY_DATA_DIR": {
      "description": "Directory holding clay.db. Leave empty to use ~/.clay-backend — the same default clay-webhook-daemon uses, so the plugin and the daemon share one database. If you change it, pass the same path to the daemon via --data-dir.",
      "required": false
    },
    "OPENAI_API_KEY": {
      "description": "OpenAI API key for embeddings (optional — falls back to local model)",
      "sensitive": true,
      "required": false
    },
    "EMBEDDING_PROVIDER": {
      "description": "Embedding provider: 'openai' or 'local'",
      "default": "local",
      "required": false
    },
    "WEBHOOK_PORT": {
      "description": "Port for the webhook HTTP server (Clay sends data here)",
      "default": "8742",
      "required": false
    },
    "WEBHOOK_HOST": {
      "description": "Bind address for the webhook server. 127.0.0.1 (default) = this machine only; 0.0.0.0 exposes it to your network — set WEBHOOK_API_KEY if you do",
      "default": "127.0.0.1",
      "required": false
    },
    "WEBHOOK_API_KEY": {
      "description": "API key to protect the webhook endpoint (local mode with ngrok)",
      "sensitive": true,
      "required": false
    },
    "REMOTE_URL": {
      "description": "URL of hosted Clay backend service (e.g. https://your-app.railway.app). If set, plugin uses remote mode instead of local SQLite.",
      "required": false
    },
    "REMOTE_API_KEY": {
      "description": "API key for the hosted service",
      "sensitive": true,
      "required": false
    }
  }
}
